import 'package:flutter/material.dart';

class AppMusicWidget extends StatelessWidget {
  const AppMusicWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
